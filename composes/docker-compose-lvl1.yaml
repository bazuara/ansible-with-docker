version: "3"
services:
    ansible_tower:
        image: ansible_tower
        ports:
            - "8022:22"
        restart: "no"
        volumes:
            - ./ansible_tower/inventory:/etc/ansible/
        container_name: "ansible_tower"
        hostname: "ansible_tower"
